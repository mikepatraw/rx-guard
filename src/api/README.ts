// API entrypoints
//
// Current local endpoints:
// - GET /health
// - POST /review
// - POST /byo-a2a/invoke
//
// The /review endpoint is the stable internal review interface for local MVP use.
//
// The /byo-a2a/invoke endpoint is a provisional adapter endpoint intended to help
// shape RX Guard toward the Prompt Opinion BYO Agent / A2A integration path without
// coupling the core review engine directly to platform-specific transport assumptions.
